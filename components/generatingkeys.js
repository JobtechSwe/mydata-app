import React, { Component } from 'react'
import { Text } from 'react-native-paper'

export default class GeneratingKeys extends Component {
  render() {
    return (
      <Text label="Generating keys">Generating keys</Text>
    )
  }
}